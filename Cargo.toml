[package]
name = "vroom"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
avr-device = { version = "0.3", features = ["atmega1284p", "rt", "avr-device-macros"] }
